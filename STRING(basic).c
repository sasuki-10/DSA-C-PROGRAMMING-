#include<stdio.h>
int main()
{
	char s[100],i;    //100 is the size of char array 
	printf("ENTER THE STRING:",i);
	scanf("%s",s[i]);              //%s is used for string.....
  //gets() can also be used in place of scanf..........
	return 0;
}
